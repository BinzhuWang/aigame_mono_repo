
## 一、Docker Compose 服务搭建及功能说明

### 1. `aigame-backend`
基于 `rockylinux` 镜像构建，使用 Rust 语言和 `Actix-web` 框架开发。负责处理 AI 驱动小游戏生成的核心业务逻辑，与数据库、消息队列交互，为前端和其他服务提供 API 支持。依赖 `aigame-mysql` 存储数据，依赖 `rabbitmq` 进行异步消息处理。

### 2. `aigame-tools`
基于 Python 构建的工具包服务。提供文档解析（如 PPT、PDF 等格式）、消息队列处理、文本转语音（借助 Microsoft Azure 服务）以及身份验证（JWT 编码和用户登录）等辅助功能。依赖 `rabbitmq` 进行消息通信，依赖 `documentserver` 处理文档。

### 3. `aigame-mysql`
使用 `mysql:8.0` 官方镜像，作为项目的数据库。负责持久化存储游戏案例、附件信息等各类业务数据，为后端服务提供稳定的数据支持。

### 4. `rabbitmq`
实现消息队列功能，支持不同服务之间的异步通信。提高系统的并发处理能力和响应速度，确保服务之间的解耦和高效协作。

### 5. `documentserver`
使用 `onlyoffice/documentserver` 镜像，提供文档处理和转换服务。支持多种文档格式，满足项目中文档解析、转换等操作需求。

## 二、`aigame-backend` 完成的路由及功能

### 1. 附件管理路由
- 处理文件上传操作，将文件存储到指定位置。
- 查询附件的相关信息，如上传时间、大小等。
- 更新附件的状态，如标记为已处理、已删除等。
- 生成附件的截图，方便预览。

### 2. 游戏管理路由
- 创建新的游戏案例，将游戏的基本信息保存到数据库。
- 查询游戏案例的详细信息，包括游戏规则、状态等。
- 触发游戏生成流程，将生成请求发送到消息队列进行异步处理。
- 更新游戏的状态，如开发中、已上线等。
- 获取游戏模块的相关信息，帮助了解游戏的结构。

### 3. 系统状态路由
提供系统的运行状态信息，方便管理员监控系统的健康状况，如服务是否正常运行、资源使用情况等。

### 4. 聊天交互路由
支持用户与系统进行实时聊天交互，提供聊天流功能，可应用于游戏辅助、客服等场景。

### 5. 用户管理路由
处理用户相关操作，如用户登录、管理用户的身份信息和权限，保障系统的访问安全。 
## 三、tools 模块实现的核心功能
### 1. 消息队列处理
- 实现与 rabbitmq 的交互，负责消费来自后端服务的生成任务消息
- 将处理结果通过消息队列异步返回给后端服务
- 支持失败任务的重试机制
### 2. 文档解析引擎
- 支持 PPT/PDF 等格式的文档解析
- 提取文档中的文本内容和元数据
- 识别文档中的结构化数据（如章节标题、列表项等）
### 3. 多媒体处理
- 文本转语音功能（TTS）集成 Microsoft Azure 服务
- 支持生成多种语音风格和语速的音频文件
- 提供音频元数据提取能力
### 4. 身份认证系统
- 基于 JWT 的令牌生成与验证
- 管理用户会话状态
- 提供 API 接口的访问权限控制
### 5. 代理服务层
- 封装第三方服务（如 OpenAI）的调用
- 提供重试机制和错误处理
- 实现请求的负载均衡和流量控制